# 吨吨记上线交接（2026-09-05）

## 当前结论

正在接入商业服务，尚未正式上线。不要把本地测试、旧 APK 或已有下载页构建当成登录、支付、云同步、AI 全链路已验收。本文不含密钥。

## 已找回的产品决定

来源：`mobile/docs/mature-stage-status.md` 的“已冻结的产品决定”，并与会员页、服务端权益规则一致。

- 首发中国大陆；手机号登录，并新增微信、QQ 授权登录需求。
- 记录、普通修图、普通滤镜永久免费，端侧处理。
- 月会员 ¥18、年会员 ¥128；首次赠 1 次 AI，会员每天 1 次，北京时间 00:00 恢复，不累计，确定失败返还。
- AI 沿用火山方舟 Doubao-Seedream-5.0-lite；具体可用模型 ID 与实时费用需控制台核验。旧文档约 ¥0.22/张不是当前报价承诺。
- 网站用途是介绍和下载安装包，不能把浏览器演示登录当成 App 的真实登录。
- 老 PRD 的“每月 12 次、结转 24 次”是未确认建议，已被后来的每日额度方案替代。

## 本轮代码工作

- 服务端接入官方火山短信 SDK，包含消息组、审核签名、模板和变量配置；关闭 SDK/HTTP 自动重试；失败信息脱敏；未配置仍返回 503。禁止真实测试使用假成功。
- 验证码消费与错误次数改为原子更新，处理手机号并发注册唯一冲突。并发测试覆盖重复提交、5 次错误、正确/错误竞争等。
- 手机端已实现真实验证码请求/验证、倒计时、防重复提交、换号失效、本机退出和 Keychain/Keystore 安全会话存储；新增 `react-native-keychain@10.0.0`。生产 API origin 尚未填写，未发真实短信验证。微信/QQ 需要真正的开放平台 SDK/审核参数，不可伪造登录。
- AI 请求修复 `/api/v1` 路径与 Bearer 认证，未登录不上传照片，API 地址只接收无凭据/查询串的 HTTPS origin。React Native fetch 不支持标准的 redirect 控制参数，服务端网关必须禁止认证/AI路由向其他地址重定向，正式包需原生层验证。
- 下载页修复上次中断残留的无效 CSS，补齐奶油色页面样式、响应式和减少动画偏好；更新吨吨记标题。已完成构建与本地 HTTP 200 检查。

验证：服务端完整 50 项离线测试通过（含验证码并发及短信 SDK 请求测试）；手机端 lint、TypeScript、26 套/130 项测试通过；下载页生产构建通过。本轮未生成新版 APK/IPA，未发布网站，已关闭本轮本地预览服务。不要把单元测试当成支付或真机验收。

## 火山引擎连接

- veFaaS CLI 0.3.1 已通过官方版本检查。
- 用户已完成官方 SSO 授权，凭据保存在系统钥匙串。后续 CLI 调用必须加 `--keychain`；不加时会错误地读取另一种存储并提示未登录。
- cn-beijing 账号现有 8 个其他项目应用，没有吨吨记应用。
- 查询到唯一网关 `bakemate-gateway`，ID `gda76hv0t7m0sepu6j40g`。不能默认为吨吨记专用网关，不要修改其他项目或共享路由；复用前需明确确认影响范围。
- 未创建付费资源、未发送真实短信、未调用付费生图、未扣款。
- 浏览器自动控制因插件更新仍引用已移除的旧版本服务文件而失败，重置 JavaScript 运行时未恢复。CLI 官方登录成功不代表浏览器控制已恢复。需要重启 Codex 后重连，禁止复制/改写插件绕过。

## 正式上线阻塞清单

用户随后确认家里公司从事机械租赁，并同意作为吨吨记拟运营和收款主体，已提供执照照片。照片未列软件相关经营项目；用户希望优先核对不增项能否使用，并明确要求代为推进。支付宝认证状态、平台应用、商户及公司最新登记状态仍未知。该同意不等于可未经确认上传执照、变更工商登记或完成签约。

申请准备已写入 `docs/支付宝申请准备_吨吨记.md`，包含支付宝与登记机关咨询草稿、产品说明、真实未完成项和操作边界。没有对外发送或提交。最新浏览器连接检查仍报旧版本服务文件缺失，不能代点支付宝页面，需要恢复浏览器连接后继续。

用户随后追问“不能申请吗，支付宝收款挺简单”：不要把“还没申请”说成“不能申请”，也不要自行把产品改为只做免费版。支付宝 App 支付官方条件是实名认证支付宝账户、同主体真实营业执照；未上架 App 可提交 demo 或产品说明。个人收款/当面付的条件不等于 App 支付条件。应继续准备申请资料和技术联调，真实身份、签约和提交审核由用户确认；不能伪造线下门店来申请当面付。

官方依据（本轮已查阅）：

- [支付宝 App 支付申请条件](https://help.alipay.com/enterprise/knowledgeDetail.htm?knowledgeId=201602169764)
- [支付宝不同支付产品签约条件](https://help.alipay.com/enterprise/knowledgeDetail.htm?knowledgeId=201602191944)
- [Apple 数字功能/订阅支付规则](https://developer.apple.com/app-store/review/guidelines/#in-app-purchase)：中国区 iOS 的 AI 会员按内购接入，不能直接套 Android 支付宝方案。

进一步核验：微信移动应用审核规范存在个人主体/个人类目，QQ 允许个人申请开发者，Apple 允许个人开发者入驻；不能将没有公司误判为所有登录和内购均无法申请。具体应用与权限仍须实际审核。

- [微信移动应用审核规范](https://developers.weixin.qq.com/doc/oplatform/Mobile_App/operation.html)
- [QQ 成为开发者](https://wiki.connect.qq.com/成为开发者)
- [Apple 个人开发者注册](https://developer.apple.com/programs/enroll/)

1. 短信：消息组 ID、审核签名、审核模板及最小 SendSms 权限服务端凭据；增加发送侧并发/IP/预算防刷，真实测试需事先明确费用与接收号码。
2. 身份：微信、QQ 已审核移动应用、App ID、正式 Android 签名、iOS 回调/Universal Link、服务端授权校验与同账户绑定。iOS 审核需核对等价隐私登录选项（通常 Apple 登录）。
3. 会话：后端当前仍是 30 天签名 token，尚无持久化会话撤销和账号注销；本机退出不能宣称已撤销服务端 token。
4. 数据库：生产禁用 SQLite；需独立生产 PostgreSQL、驱动、迁移、备份和用户隔离。不能借用其他项目数据库。
5. 存储：TOS 私有桶给 AI/用户照片，发布包另用专门的下载对象；目前没有日记云同步接口，不能承诺换机恢复。照片不要设公开读。
6. AI：远端 job ID 持久化、已完成结果下载/本地入库、轮询及重启恢复还未接通；当前后端同步生成，未知超时不能直接返额度，需持久任务恢复及原子额度账本。现有代码不应公开商业运行。
7. 付款：需要审核主体、支付商户或 App Store Connect 商品及服务器验签、幂等订单、续订、退款和恢复购买。按钮和展示价格不是已接入支付。
8. 安装包：旧 Android 包为 Debug 测试签名，不能当正式稳定版。新安全存储原生依赖还需 iOS Pods 与双端构建/真机测试。iOS 未有可用发布签名或最新 IPA。
9. 下载站：APK 约 236 MiB，不应进入 Git 或 Sites 静态目录；待对象存储下载地址与版本/SHA-256验证。下载页当前 CTA 仍是页内跳转，不是真实安装包下载。

## 后续执行顺序

先核对家里公司的授权/平台申请条件，并行准备申请 → 完成账户与生产存储 → AI 结果/额度全链路 → 支付与沙箱验收 → 正式签名包 → 开放下载站。网站可并行设计，不以页面完成代替商业服务验收；用户没有同意改为仅免费内测，不要擅自取消商业上线目标。

遵循根目录上线部署手册及 veFaaS 技能。首轮 `vefaas inspect` 对 backend 检测到了无效 `buildCommand`（只有引号），部署前必须显式修正并验证。生产必需环境变量配置完毕后才允许发布，不为绕过启动保护使用开发模式。

`web-demo/.openai/hosting.json` 已有 Sites project_id，必须复用，不创建重复网站。当前未发布本轮改版；恢复浏览器后可继续预览。公开发布前核对下载链接和实际访问范围。
