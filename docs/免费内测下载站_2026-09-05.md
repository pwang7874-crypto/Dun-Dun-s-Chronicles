# 免费内测下载站交接

- 用户决定：先做免费内测，暂停支付；先完成前端，下载存储费用确认后再接下载。
- 已完整阅读《AI Agent 产品上线部署手册_副本.md》。本轮沿用现有 Sites 网站发布私人预览，不创建火山云收费资源；后端及对象存储仍须另行核实权限、价格和上线条件。
- 网站目录：`web-demo/`。新增 Android / iOS 独立进度卡片、小酱油点击回应、App 实际截图切换、心跳/浮动/按压反馈、安装问答、减少动画支持。
- 预览：https://sipnote-drink-diary.pwang7874.chatgpt.site 。仅网站所有者可见，外部无登录请求返回 401，不能作为公众下载站宣传。
- Sites 版本 3，源码提交 `928466fe30f1d3858e1f7af920f17fc573f5edba`，部署成功。build / TypeScript / lint 均通过。
- 浏览器工具不可用，未完成浏览器视觉或交互实测。参考网址本轮打开失败，沿用已有奶油风与用户素材，未声称精准复刻。
- 两个平台按钮均为明确的未开放状态，无假下载链接。Android 旧测试包约 236 MB，测试签名，需发布验收和存储；iOS 还没有可分发包。
- 网站不收费、不收集照片或登录信息；AI 标注尚未开放，计划后续邀请码限量测试。尚未实现 App 的完整免费内测云端链路，不要混同网站完成和 App 已正式上线。

## iOS

- 用户确认没有付费 Apple Developer 会员。当前机器 `security find-identity -v -p codesigning` 返回 0 个有效签名身份，工程无 Development Team。
- 当前 iOS JS Release bundle 已成功输出到 `/private/tmp/dundunji-ios-free-beta.jsbundle`，这不是 IPA，不可直接安装到 iPhone。
- 工程使用项目自带 Ruby：`mobile/.toolchains/ruby-3.3.12-final/bin`；不要用系统 Ruby 的 bundle，版本不匹配。
- 原生依赖同步已成功，RNKeychain 10.0.0 已进入 Podfile.lock。项目自带 Ruby + 联网执行 pod install 成功；系统 Ruby 版本不匹配，受限网络首次尝试失败。
- Release iOS Simulator 原生构建成功（xcodebuild exit 0），包含 arm64 / x86_64。应用路径 `/private/tmp/dundunji-ios-free-beta-build/Build/Products/Release-iphonesimulator/DrinkDiary.app`；这是模拟器应用，不是可供 iPhone 安装的 IPA。未进行模拟器启动或真机交互测试。
- 发布前仍需处理 iPad 152 / 167 图标缺失警告，并复查隐私清单、第三方 SDK、数据存储与免费内测能力。未把编译成功当作功能全部验收。
- 后续正式分发需用户自行注册/登录 Apple Developer、批准付费和协议，再配置真实团队、签名及分发审核。未代用户付费或注册。
