# 浏览器修复与国内上线交接（2026-09-05）

## 当前用户要求

- 吨吨记下载站先做免费内测，Android / iOS 两个入口；前端已完成并私有发布。
- 用户明确要求国内上线，称已有备案域名并已登录火山云，但尚未提供具体域名；不能猜测或修改其他网站 DNS。
- 安装包存储费用确认后再接下载。没有授权未经告知的收费资源创建。

## 已完成

- 网站与 iOS 构建详见 `免费内测下载站_2026-09-05.md`。
- veFaaS CLI 0.3.1 已核对最新；`vefaas login --check --keychain` 成功。
- 只读查到的网关为 `bakemate-gateway`，属于其他项目，未修改或复用。
- 现有 Sites 仍仅所有者可见，不是国内托管；没有将其公开冒充国内上线。

## 浏览器修复实证

- 用户明确授权修复缺失组件。
- 读取 OpenAI Docs、Plugin Management 技能与浏览器故障文档，通过官方文档和本机 CLI 帮助排查。
- `codex plugin list --marketplace openai-bundled --json` 确认 Browser 已安装并启用，版本 26.901.41123。
- 新版 `scripts/browser-service.mjs` 实际存在。
- 执行 `codex plugin add browser@openai-bundled --json` 成功，安装位置与版本一致；未卸载、未清理浏览器数据。
- 重置 Node 会话后重新初始化仍失败：当前 trusted-worker 尝试导入已不存在的旧版 `browser/26.825.51511/scripts/browser-service.mjs`。
- 未手改 runtime、未伪造旧目录或绕过可信路径检查。
- 官方插件说明要求安装后开启新聊天/会话以使用工具：https://learn.chatgpt.com/docs/plugins 。当前需新会话验证，不能保证已修复。

## 新会话下一步

### BrowserAct 最新进展（2026-09-05）

- 用户安装 BrowserAct 插件后，配套 CLI 1.4.2 已安装于 `/Users/a1234/.local/bin/browser-act`，已完整读取 core / advanced 操作说明。
- 用户明确同意本机 Chrome 直连，创建 `dundunji-volcengine`，ID `direct_local_116539362641969344`。未导入浏览器资料，未购买云浏览器。
- 尝试以会话名 `dundunji-launch-0905` 打开火山云，返回 Error 230325：Chrome 未开启远程调试，未发现 DevToolsActivePort。尚未读取控制台页面。
- 工具要求先告知并取得用户同意，才能关闭 Chrome、更新 Chrome Local State 并重启。尚未执行该操作。
- 用户同意重启后，工具给出的重试命令为：`/Users/a1234/.local/bin/browser-act --session dundunji-launch-0905 browser open direct_local_116539362641969344 https://console.volcengine.com/ --allow-restart-chrome`。
- 没有修改 DNS、创建付费资源、配置 AI 密钥或执行云端部署。

### Chrome 重启后验证成功（2026-09-05）

- 用户已明确许可关闭并重启 Chrome；带 `--allow-restart-chrome` 的首次打开返回 Error 230404（Unknown error），当时会话列表为空。
- 随后不带重启参数重试成功，会话 `dundunji-launch-0905` 实际读取到 `https://console.volcengine.com/home`，确认控制台已登录。
- 点击可见“备案”入口，读取 `https://console.volcengine.com/beian/overview/`；“我的备案”当前显示欢迎页面和“开始备案”，未显示已有备案域名清单。这不能证明用户在其他服务商没有备案。
- 下一步需要用户提供吨吨记拟使用的具体域名，再核对备案及接入条件。不得猜测域名或直接提交备案。
- 浏览器连接已恢复，不再要求重装插件。仍未修改 DNS、创建收费资源或完成国内上线。

### 域名选型与报价（2026-09-05）

> 最新授权：历史资源核对后，用户已回复“好”，批准购买 `dundunji.cn`，首年不超过 36 元、不自动续费，并继续准备备案。必须先核对实时结算金额与实名模板；不得默认使用其父亲公司主体。不包含服务器、下载流量或其他新费用授权。

- 获批后尝试会话 `dundunji-register-0905` 两次连接均返回 Error 230404（Unknown error）；首次失败后会话列表为空，未实际进入任何下单页面。没有购买、付款或提交实名材料。不要重复要求用户批准同一 36 元预算；先恢复浏览器连接。

- 用户委托选择域名，首选 `dundunji.cn`。
- 已通过 BrowserAct 新会话 `dundunji-domain-0905` 在火山云域名注册页实际查询；结果显示 `dundunji.cn` 可加购，报价 36 元/首年，标注新用户活动。续费价及最终结算价尚未核对。
- `dundunji.com` 查询显示已注册，不走委托收购。
- 查询入口：`https://console.volcengine.com/domain-service/search`。未加购物车、下单、付款或开启自动续费。下一步等待用户确认首年不超过 36 元的购买预算，再核对结算及实名材料。

### 找回历史“域名”线索（2026-09-05）

- 已搜索桌面项目文档及用户授权查找的历史项目对话。
- 在 `2026/08/25/rollout-2026-08-25T17-03-28-01a03829-0961-7d00-971d-873f0aa8ab9c.jsonl` 的“一个人的烘焙搭子”记录中，用户原话为：`gda76hv0t7m0sepu6j40g这个是我域名id`。
- 同段历史助手核对结果明确为 `bakemate-gateway` 的 Serverless API 网关 ID，与之前只读查询结果一致；不能视为已购自定义域名或 ICP 已备案证据。
- 当前火山云“域名列表”页面显示全部域名 0 个；仍不排除其他账号/注册商的域名。未发现具体自有注册域名名称。
- 后续优先核对已有网关接入方案、隔离要求及官网用途的备案要求，不能覆盖烘焙项目路由，也不能直接以默认网关地址宣称国内正式网站合规上线。

### 国内部署预检与静态产物（2026-09-05）

- veFaaS 0.3.1 最新版本检查通过，Keychain 授权有效。
- 只读列举已有网关的全部 8 个服务：烘焙、导演、灯光、健身项目各自前后端；所有 `CustomDomains` 均为空。吨吨记尚无应用/路由，未覆盖旧项目。
- `vefaas inspect` 把网站识别成静态 Vite，默认输出 `dist`，但原产物是 Cloudflare Worker，不能直接照默认 Caddy 配置部署。
- 已添加可选 `npm run build:mainland`，通过 `DUNDUNJI_STATIC_EXPORT=1` 开启 vinext 静态导出并关闭服务端图片优化。默认 Sites 构建与 `.openai/hosting.json` 保持不变；无需增加依赖。
- 静态构建成功，`dist/client/index.html`、`404.html`、RSC 与 JS/CSS/图片齐全；首页标题及 8 个本地引用资源检查通过。首次图片检查发现 `/_next/image` 依赖，已修复并重新构建验证。
- 国内静态部署应仅上传 `web-demo/dist/client`，不是 `dist` 根目录；产物包 `/private/tmp/dundunji-mainland-static-20260905.tar.gz`。没有上传任何服务器中间文件或密钥。
- 官网公开上线依然被备案/域名及未确认费用阻塞；不创建收费资源。官方备案场景说明：https://www.volcengine.com/docs/6428/68721?lang=zh （2026-01-14 更新），不能依据旧手册笼统声称默认网关免备案。
- 函数按量价格已按官方文档核对：https://www.volcengine.com/docs/6662/1269135 （2025-08-27 更新）；网关响应流量另行计费，尚未取得当前网关完整报价，因此未给出虚假的总价上限。
- 本轮没有重新发布 Sites；用户指定的是中国大陆部署，原私有预览仍在。不将本地产物说成已上线；Android 下载存储、iOS 分发签名、AI 接入仍未完成。

### 最新连接验证

- 新会话已按新版 Browser 技能初始化成功，旧版模块缺失错误不再出现。
- 选择火山云 URL 时返回 `No browser is available`；按官方故障流程查询 `agent.browsers.list()`，结果为 `[]`。
- 当前阻塞变为没有浏览器连接，而非缺失安装组件。保留现有 agent，不重复安装或重置。
- 需要用户打开当前应用的内置浏览器，或在 Settings → Computer use 连接外部浏览器后再验证。尚未读取火山云域名页面，未修改 DNS、未新建收费资源。

1. 检查最新 Browser 插件位置并按官方初始化流程验证连接，成功后读取完整浏览器 API / 安全与确认说明。
2. 读取用户已登录的火山云页面，确认域名及备案状态。域名目标仍未知，务必先定位。
3. 依照 veFaaS 技能与部署手册核对国内部署路径、实际费用与网关隔离。创建收费资源前告知并确认。
4. 只部署下载站前端，下载包、AI、支付与 iOS 对外分发均不能冒充已完成。
