<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Debug-only: Metro serves the JavaScript bundle over the local network. -->
    <uses-permission android:name="android.permission.INTERNET" />
</manifest>
